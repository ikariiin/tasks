import React from "react";
import { BrowserRouter, Routes } from "react-router-dom";

export const AuthRouter = () => {
  return (
    <BrowserRouter>
      <Routes></Routes>
    </BrowserRouter>
  );
};
